{
  "name": "topico-5",
  "version": "1.0.0",
  "description": "Retorna o dia da semana dado uma data via linha de comando",
  "main": "app.js",
  "scripts": {
    "test": "qunit test/tests.js",
    "babel": "babel-node app.js",
    "make": "browserify test/day-test.js -o test/tests.js -t [ babelify --presets [ env ] ]"
  },
  "author": "Lincon Dias",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "qunit": "^2.6.0"
  },
  "dependencies": {
    "dayofweek-lincon": "^1.0.0"
  }
}
